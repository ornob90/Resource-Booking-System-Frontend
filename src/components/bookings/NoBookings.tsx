import React from "react";

const NoBookings = () => {
  return (
    <section className=" h-60 w-full border shadow-sm border-gray-200  rounded-xl flex justify-center items-center">
      <p>{`You Don't have any bookings. Try create one?`}</p>
    </section>
  );
};

export default NoBookings;
